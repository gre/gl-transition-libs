// Vendored from bezier-easing-editor@0.8.0 (MIT, see LICENSE) with fixes for
// React 19: no string refs, no componentWillUpdate, no prop-types.
// The `uncontrollable` wrapper was dropped: this export is the controlled
// editor (pass `value` and `onChange`), which is how this website uses it.
export { default } from "./BezierEditor.jsx";
