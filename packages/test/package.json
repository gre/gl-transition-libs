{
  "name": "gl-transition-test",
  "private": true,
  "scripts": {
    "test": "rm -f test_results.json && gl-transition-transform -d node_modules/gl-transitions/transitions -o test_results.json && test -s test_results.json"
  },
  "dependencies": {
    "gl-transition-scripts": "^1.18.0",
    "gl-transitions": "*"
  },
  "version": "1.18.0"
}
