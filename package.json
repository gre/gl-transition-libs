{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --hoist --nohoist=headless-gl --nohoist=gl-transitions",
    "start": "cd packages/website && yarn start",
    "watch": "lerna run watch --stream --no-sort",
    "build-website": "cd packages/website && yarn build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "name": "gl-transition-libs",
  "version": "0.0.0"
}
